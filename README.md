# Cake Design System Skill

<p align="center">
  <img src="https://img.shields.io/badge/components-29-blue?style=for-the-badge" alt="29 Components">
  <img src="https://img.shields.io/badge/tokens-260+-green?style=for-the-badge" alt="260+ Tokens">
  <img src="https://img.shields.io/badge/frameworks-React_|_Vue_|_HTML-purple?style=for-the-badge" alt="React | Vue | HTML">
  <img src="https://img.shields.io/badge/figma_variants-2717-orange?style=for-the-badge" alt="2717 Figma Variants">
  <img src="https://img.shields.io/badge/python-3.x-yellow?style=for-the-badge&logo=python&logoColor=white" alt="Python 3.x">
</p>

<p align="center">
  An AI skill that enforces the <b>Cake Design System</b> (Lenovo Enterprise UI) across React, Vue, and HTML projects.<br>
  All UI code generated by AI assistants will strictly follow Figma design tokens — zero hard-coded values.
</p>

<p align="center">
  <a href="preview/cake-preview.html">📖 Live Preview Portal →</a>
</p>

---

## What It Does

When this skill is active, your AI coding assistant (Claude Code / Cursor / Copilot) will:

1. **Only use Cake CSS variables** — `var(--fdn-color-palette-*)`, `var(--sem-color-*)`, never hard-coded hex
2. **Follow the fixed spacing scale** — `var(--fdn-spacing-space-{0..80})`, no arbitrary pixel values
3. **Apply correct border-radius** — `var(--fdn-radius-radius-{4,5,8,100,128})`
4. **Use proper shadows** — `var(--fdn-shadow-shadow-{1..5})`
5. **Match Figma variant structure** — Button(Filled/Outlined/Text × Default/Success/Error × sm/md)
6. **Reference component templates** — 29 components with pre-built React/Vue/HTML code

### Before (without skill)

```css
/* AI generates arbitrary values */
.button {
  background: #0F172A;
  color: white;
  padding: 10px 18px;
  border-radius: 6px;
}
```

### After (with skill)

```css
/* AI enforces Cake tokens */
.cake-btn--filled {
  background: var(--sem-color-bg-brand);
  color: var(--sem-color-text-inverse);
  padding: var(--fdn-spacing-space-8) var(--fdn-spacing-space-16);
  border-radius: var(--fdn-radius-radius-4);
}
```

---

## Token Architecture

The Cake Design System uses a **three-layer token architecture**:

```
┌─────────────────────────────────────────────────────────────┐
│  Foundation (--fdn-*)                                       │
│  33 colors · 15 spacings · 5 radii · 5 shadows · 39 fonts │
│  Raw design values from Figma                               │
├─────────────────────────────────────────────────────────────┤
│  Semantic (--sem-*)                                         │
│  26 semantic aliases                                        │
│  bg-brand, text-inverse, border-error, state-success...    │
├─────────────────────────────────────────────────────────────┤
│  Component (cmp.*)                                          │
│  260+ component-level tokens                                │
│  button.primary.bg, modal.shadow, table.row.hover...       │
└─────────────────────────────────────────────────────────────┘
```

### Key Semantic Tokens

| Variable | Hex | Usage |
|----------|-----|-------|
| `--sem-color-bg-brand` | #0F172A | Primary brand bg, filled buttons |
| `--sem-color-text-inverse` | #FFFFFF | Text on dark backgrounds |
| `--sem-color-interactive-primary-hover` | #1D4ED8 | Hover state |
| `--sem-color-border` | #737373 | Default borders |
| `--sem-color-border-error` | #B91C1C | Error borders |
| `--sem-color-state-success` | #F0FDF4 | Success bg |
| `--sem-color-state-error-light` | #FFF1F2 | Error bg |
| `--sem-color-bg-disabled` | #F5F5F5 | Disabled state |
| `--sem-color-text-disabled` | #737373 | Disabled text |

---

## Components (29)

All 29 components from Figma, each with **React (.tsx)**, **Vue (.vue)**, and **HTML (.html)** templates:

<details>
<summary><b>Controls (5)</b></summary>

| Component | Variants | Key Tokens |
|-----------|----------|------------|
| **Button** | Filled / Outlined / Text × Default / Success / Error × sm / md | `--sem-color-bg-brand`, `--fdn-radius-radius-4` |
| **Checkbox** | Checked / Unchecked / Indeterminate × normal / disabled | `--sem-color-interactive-primary`, `--fdn-radius-radius-4` |
| **Radio** | Checked / Unchecked × normal / disabled | `--sem-color-interactive-primary`, `--fdn-radius-radius-128` |
| **Switch** | Text / Icon / Icon-only × 2-3 options | `--sem-color-bg-brand`, `--fdn-radius-radius-100` |
| **Toggle** | On / Off × normal / disabled | `--sem-color-interactive-primary`, `--fdn-shadow-shadow-3` |

</details>

<details>
<summary><b>Data Display (5)</b></summary>

| Component | Variants | Key Tokens |
|-----------|----------|------------|
| **Card** | Vertical / Horizontal / Compact × hoverable | `--fdn-radius-radius-8`, `--fdn-shadow-shadow-1` |
| **Drawer** | Left / Right × overlay / push | `--fdn-shadow-shadow-5`, `--fdn-spacing-space-24` |
| **Label / Tag** | Solid / Soft / Outline × 7 colors | `--fdn-radius-radius-4` |
| **Loading** | Progress / Spinner / Steps / Skeleton | `--sem-color-interactive-primary` |
| **Table** | Filter / Expand / Sticky / Selection × sm / md / lg rows | `--fdn-color-palette-f1f5f9` (hover) |

</details>

<details>
<summary><b>Data Input (8)</b></summary>

| Component | Variants | Key Tokens |
|-----------|----------|------------|
| **Calendar** | Date / Month / Year / Range | `--sem-color-bg-brand` (selected) |
| **Dropdown** | Single / Multi / Tree / Tag × default / focus / error | `--fdn-shadow-shadow-1` (panel) |
| **Qty Selector** | sm / md × editable | `--sem-color-border` |
| **Search** | Basic / Category / Suggestions | `--sem-color-border-focus` |
| **Slider** | Continuous / Discrete / Range | `--fdn-shadow-shadow-3` (thumb) |
| **TextField** | Text / Icon / Label / Caption / Textarea × default / focus / error / disabled | `--sem-color-border-error` |
| **Tree** | Container / Flat × checkable | `--fdn-color-palette-e0f2fe` (selected) |
| **Uploader** | Button / Drag × default / uploading / failed | `--sem-color-state-success-light` |

</details>

<details>
<summary><b>Feedback (4)</b></summary>

| Component | Variants | Key Tokens |
|-----------|----------|------------|
| **Badge** | Dot / Count / Icon × info / error / success / warning | `--fdn-radius-radius-100` |
| **Modal** | Confirm / Danger / Detail × sm / md / lg | `--fdn-shadow-shadow-2`, `--fdn-radius-radius-8` |
| **Toast** | Info / Success / Error / Warning × closable | `--sem-color-state-*` |
| **Tooltip** | Top / Bottom / Left / Right × with arrow | `--fdn-color-palette-0f172a` (bg) |

</details>

<details>
<summary><b>Navigations (7)</b></summary>

| Component | Variants | Key Tokens |
|-----------|----------|------------|
| **Accordion** | Single / Multi-open × bordered / ghost | `--fdn-color-palette-e2e8f0` (border) |
| **Breadcrumb** | Above / Below title × collapsible | `--sem-color-text-link-hover` |
| **Footer** | Light / Dark × multi-column | `--fdn-spacing-space-80` (padding) |
| **Navbar** | TopBar / Menu / Panel / Utility × collapsed | `--sem-color-interactive-primary-hover` |
| **Pagination** | Full / Simple × jumper / sizer | `--sem-color-interactive-primary` (active) |
| **Stepper** | Numbered / Dot × horizontal | `--fdn-radius-radius-128` |
| **Tabs** | Horizontal / Vertical × Line / Card | `--sem-color-interactive-primary` (indicator) |

</details>

---

## Installation

### Claude Code

```bash
# Clone into your project's skill directory
git clone <your-gitlab-url> .claude/skills/ui-ux-lenovo-skill

# Or copy manually
cp -r skills/ui-ux-lenovo-skill .claude/skills/
```

### Cursor

```bash
# Copy the .cursorrules to your project root
cp skills/ui-ux-lenovo-skill/.cursorrules .cursorrules
```

### GitHub Copilot

```bash
# Copy the copilot instructions
mkdir -p .github
cp skills/ui-ux-lenovo-skill/.github/copilot-instructions.md .github/copilot-instructions.md
```

### All Platforms — Include Tokens CSS

Regardless of platform, your project must include the Cake tokens CSS file:

```bash
# Copy the CSS file to your project
cp skills/ui-ux-lenovo-skill/templates/cake-tokens.css src/styles/cake-tokens.css
```

```html
<!-- HTML: link in head -->
<link rel="stylesheet" href="cake-tokens.css">
```

```tsx
// React: import in App.tsx or main entry
import './styles/cake-tokens.css';
```

```vue
<!-- Vue: import in main.ts or App.vue -->
<script setup>
import './styles/cake-tokens.css';
</script>
```

---

## Prerequisites

Python 3.x is required for the search script (optional but recommended).

```bash
python --version
```

---

## Usage

### Automatic Mode

Once installed, the skill activates automatically when you ask AI to build UI components:

```
Create a success button component
Build a data table with selection
Design a modal for confirming deletions
```

The AI will automatically use Cake tokens and reference the component templates.

### Search Engine (Advanced)

Use the built-in BM25 search engine to find exact token combinations:

```bash
# Search components
python scripts/search.py "button success filled" --domain component

# Search colors
python scripts/search.py "primary blue" --domain color

# Search typography
python scripts/search.py "heading bold 20" --domain typography

# Search spacing/radius/shadow
python scripts/search.py "card padding shadow" --domain spacing

# Auto-detect domain
python scripts/search.py "modal confirm"
```

### Example Output

```
🔍 Domain: component  |  Query: "button success filled"  |  Results: 5

--- Result 1 ---
  Component: Button
  Variant: filled
  State: success
  BG Token: --fdn-color-palette-047857
  Text Token: --sem-color-text-inverse
  Radius Token: --fdn-radius-radius-4
  Padding Token: --fdn-spacing-space-8 --fdn-spacing-space-16
```

---

## File Structure

```
skills/ui-ux-lenovo-skill/
├── SKILL.md                            # Core constraint rules (read by AI tools)
├── README.md                           # ← You are here
├── .cursorrules                        # Cursor adapter
├── .github/
│   └── copilot-instructions.md         # Copilot adapter
├── data/                               # BM25-searchable knowledge base
│   ├── cake-colors.csv                 # 33 colors with semantic roles
│   ├── cake-components.csv             # 90+ component-variant token specs
│   ├── cake-typography.csv             # 39 typography styles
│   └── cake-spacing.csv               # Spacing, radius, shadow scales
├── scripts/                            # Search engine
│   ├── core.py                         # BM25 algorithm
│   └── search.py                       # CLI search tool
├── templates/                          # Ready-to-use code
│   ├── cake-tokens.css                 # Complete CSS variable file
│   └── components/                     # 29 components × 3 frameworks
│       ├── button/                     # react.tsx, vue.vue, html.html
│       ├── checkbox/
│       ├── radio/
│       ├── switch/
│       ├── toggle/
│       ├── card/
│       ├── drawer/
│       ├── label/
│       ├── loading/
│       ├── table/
│       ├── calendar/
│       ├── dropdown/
│       ├── qty-selector/
│       ├── search/
│       ├── slider/
│       ├── text-field/
│       ├── tree/
│       ├── uploader/
│       ├── badge/
│       ├── modal/
│       ├── toast/
│       ├── tooltip/
│       ├── accordion/
│       ├── breadcrumb/
│       ├── footer/
│       ├── navbar/
│       ├── pagination/
│       ├── stepper/
│       └── tabs/
├── references/                         # Source Figma data (read-only)
│   ├── cake-tokens.json
│   ├── cake-components.json
│   └── cake-token-draft.json
└── preview/
    └── cake-preview.html               # Interactive preview portal
```

---

## Figma Source

- **File**: cake For Enterprise (prev. Coffee)
- **Component Sets**: 61
- **Variants**: 2717
- **Pages**: Styles, Icons, Navigation, Controllers, Data Input, Data Display, Feedback

---

## Supported Platforms

| Platform | Install Method | Auto-Activate |
|----------|---------------|---------------|
| **Claude Code** | `.claude/skills/` directory | ✅ Yes |
| **Cursor** | `.cursorrules` in project root | ✅ Yes |
| **GitHub Copilot** | `.github/copilot-instructions.md` | ✅ Yes |
| **VS Code + Copilot Chat** | Reference SKILL.md in chat | Manual |
| **Any AI** | Paste SKILL.md content as system prompt | Manual |

---

## Anti-Patterns (Enforced)

| ❌ Don't | ✅ Do |
|----------|-------|
| `color: #0F172A` | `color: var(--fdn-color-palette-0f172a)` |
| `padding: 15px` | `padding: var(--fdn-spacing-space-16)` |
| `border-radius: 6px` | `border-radius: var(--fdn-radius-radius-8)` |
| `font-family: Arial` | `font-family: var(--fdn-typo-style-5-font-family)` |
| `box-shadow: 0 2px 4px rgba(...)` | `box-shadow: var(--fdn-shadow-shadow-1)` |
| Inventing arbitrary spacing | Use only the Cake spacing scale |
| Mixing other design systems | Use Cake tokens exclusively |

---

## Contributing

1. Edit CSV files in `data/` to update token knowledge base
2. Update component templates in `templates/components/`
3. Run `python scripts/search.py "test query"` to verify search accuracy
4. Update `SKILL.md` if adding new mandatory rules

---

## Credits

- **Search Engine**: BM25 algorithm derived from [ui-ux-pro-max-skill](https://github.com/nextlevelbuilder/ui-ux-pro-max-skill) (MIT License)
- **Design System**: Cake For Enterprise — Lenovo Figma Design System
